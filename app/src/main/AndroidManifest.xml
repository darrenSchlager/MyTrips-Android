<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.regis.darren.mytrips">

    <application
        android:allowBackup="true"
        android:icon="@drawable/globe"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".TripActivity"
            android:label="@string/trip_activity_name"/>
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LocationActivity"
            android:label="@string/location_activity_name" />
        <activity
            android:name=".ActivityItemActivity"
            android:label="@string/activityitem_activity_name" />
        <activity android:name=".ItineraryActivity"></activity>
    </application>

</manifest>
